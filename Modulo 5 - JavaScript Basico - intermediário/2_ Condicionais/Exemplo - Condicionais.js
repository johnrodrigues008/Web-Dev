//  Exemplos de condicionais:

// A declaração if 

if (hour < 18){
      console.log('Já passou das 18 horas!')
}

// A declaração else

if (hour < 18) {
      greeting = 'Good day';
} else {
      greeting = 'Good evening';
}

// A declaração else if 

if (time < 10) {
      greeting = 'Good morning';
} else if (time < 20) {
      greeting = 'Good day';
} else {
      greeting = 'Good evening';
}